name: morse_code_generator
description: A Flutter package to convert between English and Morse code
version: 1.0.0
homepage: https://github.com/Suneel-DK/morse_code_generator

environment:
  sdk: '>=3.4.4 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

